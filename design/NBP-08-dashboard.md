# NBP Prompt — 08 Traveler Dashboard (dashboard.html)
# Model: gemini-3-pro-image-preview (Nano Banana Pro)
# Aspect ratio: 16:9 — set in API/UI, not in prompt

A UI mockup screenshot of Provodnik's traveler personal dashboard — the management area where a user monitors active requests, joined groups, guide offers, and confirmed bookings. A four-tab horizontal navigation bar sits at the top of the main content area, below the global nav: tabs labeled "Мои запросы", "Группы", "Предложения", "Бронирования"; the active tab "Мои запросы" is underlined in cool teal. The active tab displays three trip-request cards stacked in a primary column: each card shows a square destination photo thumbnail on the left, destination name and travel dates in medium Cormorant Garamond, a thin progress bar with seat-count label, a guide-offer status badge (amber for pending, teal for confirmed), and small action links. A secondary column on the right holds a compact summary widget listing total active requests, pending guide offers, and a nudge CTA button "Создать запрос". Warm editorial ambient lighting renders the dashboard in functional clarity with no cinematic drama; status badge colors (amber and teal) are the only accent pops against the cream card backgrounds. The view is a 1440 px desktop screenshot with a content width that feels denser than the marketing pages but still spacious. Typography is Manrope throughout at slightly tighter line spacing; thumbnail photos carry the same warm travel photography treatment used across the site.
