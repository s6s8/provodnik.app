# NBP Prompt — 03 Request Details (request-details.html)
# Model: gemini-3-pro-image-preview (Nano Banana Pro)
# Aspect ratio: 16:9 — set in API/UI, not in prompt

A UI mockup screenshot of a single open-group request detail page on Provodnik, where a traveler evaluates and decides to join a forming tour group. The upper section shows a wide destination hero photograph (winter Baikal ice lit by pale arctic daylight) with the navigation bar above it; immediately beneath, a two-column layout carries a request summary panel on the left — destination name in large serif, travel dates, current vs target participant count with a warm-amber progress bar, stacked participant avatars, and a budget range — and a sticky action sidebar on the right holding a prominent rounded "Вступить в группу" button in solid teal and a card showing the estimated per-person cost if one more traveler joins. A lower section on the same page lists guide offers submitted to this group: each offer row is a compact horizontal card with a circular guide portrait, guide name, a star-rating chip, and the offered price per person, plus two small buttons — accept (teal fill) and counter-offer (outlined). Warm editorial lighting on the hero photograph; the structured panels below use clean ambient light with soft hairline card borders. The view is a straight-on 1440 px full-page screenshot with comfortable reading column width. Card surfaces are matte cream; micro-typography uses Manrope at small weight; the progress bar and primary button share the same cool teal.
