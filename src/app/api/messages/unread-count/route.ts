import { NextResponse } from "next/server";import { getUnreadCount } from "@/lib/supabase/conversations";import {  authenticateMessagesRequest,  messagesRateLimitResponse,  messagesUnauthorizedResponse,  withRateLimitHeaders,} from "@/app/api/messages/_shared";export async function GET() {  const auth = await authenticateMessagesRequest();  if (auth.kind === "limited") {    return messagesRateLimitResponse(auth.remaining);  }  if (auth.kind === "unauthorized") {    return messagesUnauthorizedResponse(auth.remaining);  }  if (auth.kind === "disabled") {    return NextResponse.json(      { unreadCount: 0, userId: null },      {        headers: withRateLimitHeaders(auth.remaining),      },    );  }  const unreadCount = await getUnreadCount(auth.userId);  return NextResponse.json(    {      unreadCount,      userId: auth.userId,    },    {      headers: withRateLimitHeaders(auth.remaining),    },  );}