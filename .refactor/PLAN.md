# Refactor PLAN — deslop Provodnik (Phase 2, awaiting approval)

**Status: PROPOSAL. No code touched. Phase 3 execution requires explicit "Go" (project control surface + wait-for-go).**

Recon: 5 read-only agents (architecture/magic · dead-code · duplication · slop · Context7 libraries) over the whole repo, reconciled against `docs/COMPONENT_AUDIT.md` (component layer, already done) and the frozen lint baselines. Prime directive: **behavior preservation** — this is subtraction, not fixes.

---

## Scoreboard estimate (NET-NEW, beyond the component audit)

The component audit already owns ~4,500–5,000 LOC of component-layer cleanup (39 dead components + 24 clusters, frozen in baselines). This plan is what recon found in the layers that audit did **not** cover — mostly the `src/lib` + `src/data` data layer.

| Category | Candidate LOC | Confidence |
|---|---|---|
| DELETE — whole dead modules (data/lib) | ~790 | high (0 importers, grep-verified) |
| DELETE — dead exports inside live modules | ~900 | high, but estimates overlap → verify per line |
| DELETE — dead flags / debris | ~40 | high |
| MERGE — net-new dups (relation-unwrap, selects, offer-actions) | ~90 | high |
| MODERNIZE — redundant compiler memo | ~100 | **low** (behavior risk — defer/selective) |
| Manifest — 4 unused deps + orphan SVGs + stale env | (deps, not LOC) | high |
| **Net-new candidate total** | **≈1,800–2,600 LOC** + 4 deps | agent estimates overlap; **each line re-verified in Phase 3** |

Honest caveat: three recon findings describe the same legacy data layer from different angles, so their LOC figures overlap — the range above is de-duplicated and conservative. **Every deletion is gated by a fresh 0-reference grep at execution time (PROVE-BEFORE-DELETE), not by these estimates.**

Anti-findings (good news, no action): **AP-011 empty-catch landmine is CLEAN** (0 `catch {}` in src); no commented-out code, no TODO/FIXME debris, no `@ts-ignore`, only 4 justified `as any`; `@supabase/ssr` already on the current `getAll/setAll` cookie API; zod v4 and Next 16 caching show no legacy idioms.

---

## Framework-magic VETO LIST (do-not-delete — a 0-ref grep will lie about these)

- **All ~104 app-router special files** under `src/app/**` (page/layout/route/loading/error/not-found + `global-error.tsx`, `sitemap.ts`, `robots.ts`, `favicon/icon/apple-icon`) — convention-loaded, never imported.
- **`src/proxy.ts`** — Next 16 renamed middleware→proxy; matched by filename. Never rename/delete.
- **`src/instrumentation.ts` / `instrumentation-client.ts`**, **`sentry.server.config.ts` / `sentry.edge.config.ts`** (loaded only via `await import()` in instrumentation.ts:10/14) — invisible to import graph.
- **`src/lib/supabase/database.types.ts`** (3547 LOC) — GENERATED by `bun run types`; never hand-prune.
- **`src/lib/supabase/middleware.ts`** — used only by proxy.ts.
- **11 string-called Supabase RPCs** (`.rpc("…")`): search_guides, record_marketplace_event, get_bidding_guides_for_request, get_traveler_booking_guide_display_profiles, get_joined_request_owner_display_profiles, send_qa_message, submit_review, counter_offer, record_request_view, count_competing_offers, open_dispute.
- **~30 SQL SECURITY-DEFINER / trigger / RLS functions** referenced only from the DB (accept_offer, custom_access_token_hook, handle_new_user, is_admin/is_guide, can_access_*, fn_enforce_*, fn_refresh_*_rating, tg_*, set_updated_at, …) — a TS-only scan will not see their callers.
- **`src/app/(home)/ai/page.tsx`** (live SEO landing, in `sitemap.ts:30`), **`src/app/dev/*`** (NODE_ENV-gated + noindex) — KEEP.
- **50 `use server` action files** — many reached via `<form action=>` / `.bind()`, so callsite scans undercount. Verify action wiring before removing any action.
- **`src/lib/flags.ts`** live flags (string/env-keyed): DISPUTES(16), FAVORITES(9), PARTNER(9), REFERRALS(6), NOTIFICATIONS(2), TOURS(1), PAYMENT(1). Do not treat flags.ts as inert.

---

## Batches (risk-ascending; run top→bottom)

Gate after **every** batch: `bun run check && bun run test:run && bun run build` must match baseline (0/0/1082/green). Red → `git checkout .` for that batch, log under BLOCKED, continue. One category per commit: `refactor(scope): action — LOC -N`.

**PROVE-BEFORE-DELETE (every deletion line):** `rg -l --word-regexp <symbol> src tests supabase` returns only the defining file → AND not in the VETO LIST above → AND not in config/CI/migrations → else reclassify MERGE or quarantine.

### Batch 1 — zero-risk deletions (dead files + dead exports) · risk LOW · ~1,700 LOC
Dead whole modules (0 importers, test-only or none):
- `src/lib/format.ts` (+ its test) — only importer is its own test. −44
- `src/lib/app-version.ts` — 0 refs; retires env `NEXT_PUBLIC_APP_VERSION`, `VERCEL_DEPLOYMENT_ID`, `VERCEL_GIT_COMMIT_SHA`. −16
- `src/lib/auth/checklist-account-emails.ts` (−14), `src/lib/auth/access-token-claims.ts` (−24; dead TS twin of the live SQL `custom_access_token_hook` — delete TS, never the SQL).
- `src/data/negotiations/` (−29), `src/data/destinations/index.ts` barrel (−1), `src/data/requests-format.ts` (−15), `src/data/favorites/active-user.ts` (−11).
- `src/data/reviews/supabase.ts` (−132), `src/data/guide-offer/supabase.ts` (−85).
- Dead data schema/type subdirs (0 external importers): `guide-listing/`, `traveler-booking/types.ts`, `listing-meals/schema.ts`, `listing-days/schema.ts`, `listing-tariffs/schema.ts`, `listing-tour-departures/schema.ts`, `review-replies/schema.ts`, `marketplace-events/client.ts`. ~−370

Dead exports inside LIVE modules (trim exports, keep the file):
- `src/data/supabase/queries.ts` — 7 dead fns: getPlatformStats:94, getUserRequests:297, getUserBookings:546, getUserFavorites:600, toggleFavorite:622, getListingReviews:655, getSimilarRequests:816. −250
- `src/lib/supabase/`: moderation getModerationCases:437 + the moderation-case CRUD subsystem & its 11 dead types; bookings getTravelerBookings:227; requests getTravelerRequest:186/getTravelerRequests:206; offers getGuideOffers:144; request-members getRequestMembers:108. ~−330
- `src/data/`: notifications listNotifications…/markNotificationRead (supabase.ts:42/67); quality getGuideQualitySnapshot/getListingQualitySnapshot (seed.ts:82/86); guide-assets 6 dead storage-reservation exports (supabase-client.ts:81/166/334/351 + 2 types). ~−320
- `src/lib/auth/role-routing.ts` 6 dead exports (:36/61/65/70 + 2 consts), `admin-access.ts` readJwtRole:21/isAdminAuthUser:31. ~−140
- Misc dead exports: pii/mask hasPii:46, utils pluralizePeopleGenitive:29, demo-session write/clear:64/70, triggers notifyBookingConfirmed:272/notifyReviewRequested:387, traveler-profile-completion isTravelerProfileSection2Complete:41, money formatRubFromMinor:17, queries/core formatCategory:353, guide-templates uploadTemplatePhoto:37/deleteTemplatePhoto:158. ~−120
- ~40 dead type/interface exports (types.ts 13 + scattered) — compile-time only, zero runtime. ~−150
- 5 confirmed-dead feature flags: FEATURE_TR_KPI, FEATURE_TR_PERIPHERALS, FEATURE_TR_HELP, FEATURE_TR_QUIZ, FEATURE_DEPOSITS (+ flags.test.ts entries). −20

**Rollback:** single deletion commit → `git revert`. **Do NOT bundle** the "stubbed WIP" exports (see Uncertain §) — split those into their own commit pending product confirmation.

### Batch 2 — manifest pruning · risk LOW
- Remove unused deps: `@fontsource-variable/inter`, `@fontsource/cormorant-garamond`, `@fontsource/geist-mono` (fonts load via `next/font/google` in layout.tsx:2), `@tanstack/react-query-devtools` (never mounted). 0 import sites each.
- Remove default Next scaffolding SVGs in `public/` (0 refs).
- Drop the env vars retired by Batch 1 from `.env.example` (8 total: 5 dead flags + 3 app-version).
**Rollback:** revert; re-`bun install`.

### Batch 3 — exact-duplicate merges · risk LOW · ~−27
- Relation-unwrap helper triplicated (`firstRelation` guide-assets:39 / `_firstRelation` moderation:252 / `first` audit/page:88 + 9 inline `Array.isArray(x)?x[0]`) → one `firstRow<T>` in `src/lib/utils.ts` (survivor, 119 importers), with a 3-line assert self-check. −22
- `GUIDE_PROFILE_STATS_SELECT` const for the select string duplicated verbatim at queries.ts:352/440. −1 (drift-prevention).
- `/form` route (permanentRedirect stub) → move to `next.config.ts` redirects() array. −4

### Batch 4 — semantic consolidation · risk MED · (mostly = audit waves, cross-referenced)
These overlap `docs/COMPONENT_AUDIT.md`; execute as its waves, not double-counted:
- Money-format bypass in 11 files → `formatRub`/`formatRubFromMinor` (audit money cluster).
- 3 plural helpers → `pluralize` (audit plural cluster).
- **Net-new:** `.toFixed(1)` rating format in 12 sites → one `formatRating(n)` (−8); profiles-mini `(id, full_name, avatar_url)` select repeated ~15× → `PROFILE_MINI_SELECT` const (−10); `submitOfferAction`/`editOfferAction` shared ~50 lines → local `parseAndValidateOfferForm` (−50).
- Auth-guard + envelope cluster (audit C-22, **updated numbers**: 44 `use server` files, ~98 inline `getUser()`, 24 unauthorized-string literals in 7 wordings, `{success}` in 29 files vs `{ok}` in 14, `createAction` still 0 callers) → adopt `createAction` + `requireUser`. Large (~−300), MED, = audit W4 / component-canon rule.

### Batch 5 — modernization (Context7) · risk MED → **DEFER or selective**
- Manual `useMemo`/`useCallback` (54 + 52 calls across ~21 files) redundant under React Compiler (`reactCompiler:true`). Context7 (react.dev preserve-manual-memoization lint): safe to remove **only** where memo is pure perf; a memo feeding a `useEffect` dep array or a non-idempotent path changes behavior. Recommend: **defer** — not worth the per-file behavior review in a deslop pass; the compiler already preserves them harmlessly.

### Batch 6 — structural moves · risk MED · SEPARATE approval
- `data-layering.md` violations: `src/data/*/supabase.ts` do I/O the rule assigns to `src/lib/supabase`. Architecture debt, not a dedup — its own session.

---

## Real bugs found (LOG only — do NOT fix in refactor commits)
- **State-machine assertion guards never wired** (`assertTransition`, `assertReviewTransition`, `assertReplyTransition`, `assertListingTransition` — 0 call sites): status transitions are currently **unguarded**. Deleting the dead asserts is behavior-neutral, but the *gap* is a real design concern → hand to product/security, don't silently delete without a note.
- **`FEATURE_TR_REPUTATION` flag**: dimension A saw 1 ref, dimension B saw 0. Conflict → verify before deletion (it's excluded from the 5 confirmed-dead flags above).
- **SOT `ERR-030` stale**: claims browser can't clear httpOnly SSR cookies; `@supabase/ssr` sets `httpOnly:false`. Already captured as S1 in the open-tasks goal.

## Uncertain / needs human judgment (Phase 2 decisions)
- **"Stubbed-but-never-wired" exports** — `toggleFavorite`/`getUserFavorites`, `notifyBookingConfirmed`/`notifyReviewRequested`, guide-template `uploadTemplatePhoto`/`deleteTemplatePhoto`, moderation-case CRUD, referral/partner/license/video row types. These are 0-ref today but look like **WIP scaffolding for pending features**, not abandoned code. Deleting erases the scaffold. **Product must confirm abandoned vs pending** before these go — quarantine, don't delete, if unsure.
- **Frozen `lint:canon` allowlist files** — legacy, tracked for the audit waves. Do not casually delete; changes there must shrink the allowlist in the same commit.
- **React Compiler memo removal** (Batch 5) — behavior risk; recommend defer.

---

## Recommended execution path
Given this project's control surface (QuantumHands executes product-code edits; Claude plans/reviews), the low-risk mechanical batches (1–3) are ideal QuantumHands handoffs — each is a self-contained, gated, revertible commit. Batches 4–6 overlap the component-audit waves and product decisions; sequence them there. **Nothing executes until "Go".**
