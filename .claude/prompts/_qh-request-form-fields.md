# Task: Форма запроса — компактные поля (3 правки)

Единственный основной файл: `src/features/homepage/components/homepage-request-form.tsx`.
Плюс обнови затронутые тесты: `src/features/homepage/components/homepage-request-form.test.tsx`.

НЕ трогать: серверный экшен, схему zod, значение `mode: "assembly"` (техническое, не UI-лейбл),
порядок/контракты других полей. Только UI этой формы.

## Правка 1 — «Сколько вас» + «Открытая группа» в одну строку (half-width)
Сейчас блок 4: число `groupSize` («Сколько вас»), под ним чекбокс-карточка
«Открыт к увеличению группы» (включает `mode: "assembly"`), под ним чекбокс
«Разрешаю гидам предлагать близкие даты и время».

Сделать:
- `groupSize` (число, лейбл «Сколько вас») и чекбокс открытой группы поставить
  в ОДНУ строку — два половинных поля (`grid grid-cols-2`, выровнять по низу/высоте).
- Переименовать лейбл этого чекбокса с «Открыт к увеличению группы» на **«Открытая группа»**
  (это UI-лейбл нового канонического термина; `mode: "assembly"` в коде не менять).
- Чекбокс-карточка должна по высоте совпадать с полем ввода и помещать лейбл «Открытая группа»
  без переноса на 375px.
- Чекбокс «Разрешаю гидам предлагать близкие даты и время» оставить отдельной строкой
  на всю ширину под этой строкой. Логику не менять.

Acceptance: на 375px и 1280px поля стоят в одну строку, ровно по высоте, лейбл умещается.

## Правка 2 — «Языки экскурсии» убрать в «Добавить детали»
Сейчас блок 5 «Языки экскурсии (необязательно)» (`LanguageMultiSelect`) стоит в основном потоке.

Сделать:
- Убрать поле языков из основного экрана.
- Перенести его внутрь раскрывающегося блока `<details>` «Добавить детали»
  (там, где `notes`), сверху над полем заметок, со своим лейблом «Языки экскурсии».
- Контроллер `requestedLanguagesField` и сабмит не менять — поле просто переезжает в разметке.

Acceptance: на основном экране поля языков нет; внутри «Добавить детали» оно появляется и работает.

## Правка 3 — Темы: 3 видимых + «Ещё темы»
Сейчас блок 7 «Темы» рендерит ВСЕ 8 тем из `THEMES` сеткой `grid-cols-2`.

Сделать:
- По умолчанию показывать только первые 3 темы из массива `THEMES`
  (История, Архитектура, Природа), сетку оставить `grid-cols-2`.
- Под ними кнопка-тоггл **«Ещё темы»** (type="button"); по нажатию раскрываются
  остальные 5 тем; повторное нажатие сворачивает (текст «Свернуть» при раскрытом).
- Если какая-то из скрытых тем выбрана — она должна оставаться видимой даже в свёрнутом
  состоянии, чтобы выбор не пропадал визуально (выбранные скрытые темы показывать всегда).
- Состояние раскрытия — локальный `React.useState`.

Acceptance: на 375px видны ровно 3 темы + кнопка «Ещё темы»; по нажатию появляются остальные;
выбранные темы не теряются.

## Verify (обязательно перед завершением)
- `bun run typecheck`
- `bun run lint`
- `bun run test:run` — обнови `homepage-request-form.test.tsx` под новые лейблы/структуру
  (старые ассерты на «Открыт к увеличению группы» и на языки в основном потоке надо поправить).
- Покажи `git diff --stat` — изменения только в форме и её тесте.
Коммить НЕ нужно — оставь рабочее дерево с изменениями, оператор проверит и запушит.
