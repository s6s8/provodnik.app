project_id = "pvd-agent-inbox-sort-filter-r3"

[api]
enabled = true
port = 54621
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54622
shadow_port = 54320
major_version = 17

[studio]
enabled = true
port = 54623

# Stamp profiles.role into JWT app_metadata on sign-in / refresh (AP-038).
[auth.hook.custom_access_token]
enabled = true
uri = "pg-functions://postgres/public/custom_access_token_hook"
