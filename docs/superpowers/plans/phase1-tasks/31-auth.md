# 31 auth (V-8) — cursorSDK
SCOPE: canon-align auth.
FILES: auth-entry-screen.tsx, auth/page.tsx.
WHAT: tone the full-bleed navy wash down to canon (mostly neutral, navy accent); add a desktop value-prop/trust panel beside the form (1280px); render ?error= params as a visible banner; keep the focused single-job flow.
VERIFY: live 1280/375; error param shows a banner.
COMMIT: `feat(auth): canon-aligned auth + trust panel + visible errors`

