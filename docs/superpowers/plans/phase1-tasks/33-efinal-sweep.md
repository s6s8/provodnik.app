# 33 efinal-sweep (E; orchestrator + Sonnet browser)
GOAL: confirm Phase 1 complete on prod.
DO: Sonnet browser subagent walks the whole spine (/ /form /listings /listings/[id] /guides /guides/[slug] /search /destinations /destinations/[slug] /auth) at 1280 + 375. Confirm: immersive heroes, ONE primary CTA each, all four states, no regressions (zero-rating / doubled-text / dead-CTA / glyphs / horizontal scroll), guide name+face on cards, zero-results lead CTA.
DONE-MARK when the sweep returns PASS. Then DM owner a sanitized summary.
