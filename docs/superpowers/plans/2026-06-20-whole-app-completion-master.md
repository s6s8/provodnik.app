# Provodnik — WHOLE-APP completion master plan (one ledger, run to DONE)

**Goal:** finish the ENTIRE remaining Provodnik redesign + functional cleanup in ONE continuous run — every logged-in surface (traveler cabinet, guide cabinet, admin) brought to the locked "Clean Trust" canon, every real functional bug fixed, deferred data fixed, verified live. No phases, no checkpoints — drive the ledger `docs/superpowers/plans/master-tasks/00-INDEX.md` to all-`[x]` then a whole-app browser sweep PASS.

**Context:** Phase 1 (the public conversion spine: home, /form, listings, search, listing-detail, guides, guide-profile, destinations, auth + P0 trust bugs + primitives) is SHIPPED & LIVE. This plan covers EVERYTHING ELSE. The design canon + primitives already exist (navy/amber/Onest tokens; `ImmersiveHero`, `ListHero`, `PageHeader`, `RatingDisplay`, `PublicGuideCard`, `LoadingSkeletons` in `src/components/shared/`). This is rollout + fixes, not invention.

## Per-page POLISH GATE (every page task must satisfy)
One job + one primary CTA (rest outline/ghost) · ~85% neutral (navy/amber/green = accents) · Onest, 8px rhythm · system `Button`/`Card`/`Badge`/`Chip` + Lucide icons — **NO emoji glyphs (✓ × + ▾ ★ →), NO inline color `style`, NO `window.confirm/alert`** · all 4 real states (loading skeleton → friendly empty → error+retry → success; never false "не найдено" while loading) · real data only (no fake counts/local-only mutations) · semantic tokens only · mobile 375px, ≥44px targets, Russian copy.

## Archetype patterns (reuse, don't reinvent)
- **Cabinet/list pages** (trips, favorites, notifications, messages, guide inbox/listings/bookings/reviews, admin queues): `PageHeader` (title + one primary action) → a clean list/grid of rows/cards (consistent row style, status via `<Badge variant>`, ≥44px row actions) → loading skeleton (`CardGridSkeleton`/`ListRowSkeleton`) → friendly `EmptyState` with a next-action → never a false empty while loading.
- **Detail pages** (bookings/[id], disputes/[id], admin detail, guide booking detail): `PageHeader` → primary action prominent, secondary actions subordinate → state machine actions as ONE primary + ghost others → error as Alert not a card → labelled form controls.
- **Form/wizard** (listings/[id]/book, dispute, review, guide/profile, contact-visibility): focused single-column card (like /auth/form), visible labels, one primary submit, inline validation, pending state, success redirect; replace native `window.confirm` with a shadcn confirm dialog/AlertDialog.
- **Admin queues** (moderation, disputes, guides, listings, bookings, audit): ONE consistent queue/table pattern — one primary row action, others in a menu/secondary; filters/search where the audit asked; correct preview links (use slug).

## STANDING AUTONOMY RULES (apply to every task — NEVER ask the owner)
- **DB changes autonomous via the mini:** `ssh sshm` (user idev), `SUPABASE_ACCESS_TOKEN=$(cat ~/.supabase/access-token) NEXT_PUBLIC_SUPABASE_URL=https://yjzpshutgmhxizosbeef.supabase.co node /Users/idev/provodnik/scripts/apply-migration-via-management-api.mjs <file>` (base64 the SQL over if not on the mini's branch); live-verify on PostgREST/RPC. Additive+rollback by default.
- **Decisions autonomous via an Opus PM persona:** any "what's best for the project" / product-shape question → dispatch an Opus PM persona agent (model: opus, framed as Provodnik's senior PM); act on its call; record it.
- **Full authority / no blockers (owner-set 2026-06-20):** do WHATEVER it takes to reach DONE — create/verify users, apply DB changes, seed data, adjust config — via the mini. NEVER stop on a blocker; route every "how/should we" through the Opus PM and proceed on its call. Escalate to the owner ONLY for an irreversible NON-DB op outside scope, or a hard 3× task failure.

## BUILD-LOOP RULES (learned the hard way in Phase 1 — obey)
- cursorSDK codes ALL product code; a Sonnet subagent (Agent, model: sonnet, subagent_type: general-purpose — `code-reviewer` doesn't exist) verifies each; orchestrator gates + merges.
- **Research-first — give cursorSDK a clean, complete brief, not a guess.** BEFORE composing each coder prompt, dispatch a Sonnet RESEARCHER (Agent, model: sonnet) that reads the REAL current page + its data deps (`src/lib/supabase/<domain>.ts`, server actions) + the canon primitives it should reuse (`src/components/shared/*`) + relevant SOT (HOT, ANTI_PATTERNS, PATTERNS, API_REFERENCE) and returns a STRUCTURED BRIEF: exact files to touch, current behavior to PRESERVE, the exact canon treatment, which shared primitives to reuse, which APIs to verify via context7, and known pitfalls. Fold the brief verbatim into the coder prompt. Researchers run IN PARALLEL across the wave.
- **context7 ON for every coder dispatch (no exceptions).** WIN dispatch = context7 ambient (`~/.cursor/mcp.json`); MINI dispatch = add `--mcp context7`. The coder prompt MUST include: "verify API usage (Next 16, Tailwind v4 @theme, shadcn/ui, Supabase) against context7 docs BEFORE writing." Drop context7 only if the owner explicitly says so.
- **Parallelism is via ISOLATED WORKTREES, not sequential work.** The corruption lesson is narrow: NEVER two cursorSDK dispatches on the SAME checkout. The FIX is to run several coders AT ONCE, each in its OWN worktree/branch (see PARALLEL FAN-OUT below). Use the fan-out by default; the only serial step is merging.
- **ALWAYS branch off `origin/main`** (`git checkout -B <branch> origin/main`) AND **`git rebase origin/main` before push** — stale local main makes a PR spuriously revert intervening merges.
- cursorSDK prompt: "git add ONLY the named files, NEVER `git add -A`/`.`". Orchestrator runs a git-diff scope gate (reject stray files) + trailer scan (`grep -i "cursor\|claude\|co-author"` must be empty on the squash message).
- Gate chain: `bun run typecheck && bun run lint && bun run test:run && bun run build`. Ship per-task: branch → cursorSDK → gates → Sonnet PASS → push (token-in-URL) → PR → CI green → `gh pr merge --squash --delete-branch` (clean own message) → live 1280/375 check on the page → mark `[x]` in 00-INDEX.md.
- Foreground cursorSDK dispatch with `set -o pipefail; source ~/.cursor-dispatch.env && export CURSOR_API_KEY` (bg shell drops the key). VERIFY via git diff, never the agent's summary. 3× fail on one task → STOP.
- **Course-correct / re-plan — never force a bad plan.** You read the REAL current page code before composing each task. If a task's listed issues or proposed fix are STALE or WRONG versus what the code actually does now (already fixed, moved, different shape), REWRITE that task's line in 00-INDEX.md to match reality, then dispatch against the corrected line. If an approach STALLS (cursorSDK no-ops, repeated Sonnet FAIL, a gate won't go green after 2 honest tries), STOP repeating the same prompt — change the approach: split the task, narrow the file set, or fix a blocking dependency first; consult the Opus PM persona if the right shape is unclear. APPEND any newly-discovered broken flow as a new ledger task and fix it in order. The ledger is LIVING — keep it true to the app; DONE is measured against the corrected ledger, not the original.

## PARALLEL FAN-OUT (multi-agent — use full throughput, safely)
Run multiple cursorSDK coders concurrently, each in an ISOLATED worktree; merge their branches ONE AT A TIME.

**Wave selection (each turn):** pick up to N unchecked tasks (default 3, max 4 — tune to machine load; the Next build is the heavy step) that (a) have all deps met and (b) have DISJOINT file sets — no two tasks in a wave may touch the same file. Infer each task's likely files from its ledger line + the REAL code BEFORE batching. A task that turns out to overlap another drops to the next wave.
- **SOLO waves (run alone, merge before any page wave):** C02 primitives + ANY task editing a shared surface — `globals.css`, `layout.tsx`, `src/components/shared/*`, tailwind config. Page tasks that only touch their own route files are the parallel bulk.
- **Order:** C01 (orchestrator audit) → C02 primitives (SOLO) → fan out C03/C04/C05 + the T/G/A/R/S page tasks in disjoint waves of N → B01/X01/P01 (orchestrator/PM, interleave) → Z01 LAST (solo, after everything merged).

**TWO EXECUTION HOSTS — use BOTH boxes for full throughput.** Spread each wave's disjoint tasks across:
- **WIN** (this box): `node ~/.claude/scripts/cursor-dispatch.mjs <prompt-file> --workspace D:\dev2\sdk-worktrees\provodnik\<task> --model auto` (context7 ambient). Worktree/junction setup below.
- **MINI** (`ssh sshm`, user idev; repo `/Users/idev/provodnik`): set up its own worktree `ssh sshm 'git -C /Users/idev/provodnik worktree add -b wt-<task> ~/sdk-worktrees/provodnik/<task> origin/main && ln -s /Users/idev/provodnik/node_modules ~/sdk-worktrees/provodnik/<task>/node_modules && cp /Users/idev/provodnik/.env.local ~/sdk-worktrees/provodnik/<task>/.env.local'`; scp the prompt file over; dispatch `ssh sshm 'cd ~/cursor-sdk && node dispatch.mjs --workspace ~/sdk-worktrees/provodnik/<task> --prompt-file <f> --model auto --mcp context7'`.
- Each host runs ITS OWN gates in its worktree (bun chain; over ssh for MINI). Orchestrator collects the diff (`git -C <wt> diff` on WIN; `ssh sshm git -C <wt> diff` on MINI) → feeds the Sonnet reviewer.
- **Push from the OWNING box, then merge from WIN:** WIN pushes token-in-URL; MINI `ssh sshm 'cd <wt> && git fetch origin && git rebase origin/main && git push "<token-url>" wt-<task>'` (mini has its own gh/credentials). Fallback if MINI can't reach GitHub: add the MINI worktree as an ssh remote and `git fetch` the branch to WIN, then push from WIN. Orchestrator runs `gh pr merge` from WIN for ALL.
- **MINI caveat:** the mini also serves dev.provodnik.app (`next dev`). Keep MINI coder/build concurrency MODEST (1–2) so heavy worktree builds don't starve the live dev server; WIN carries the larger share. Total wave concurrency = WIN (≤4) + MINI (≤2). Merges stay STRICTLY sequential regardless of host.

**Per-task worktree setup (Windows — OUTSIDE the repo root so fs-walking lint isn't polluted):**
```
git worktree add -b wt-<task> D:/dev2/sdk-worktrees/provodnik/<task> origin/main
cmd //c mklink //J "D:\dev2\sdk-worktrees\provodnik\<task>\node_modules" "D:\dev2\projects\provodnik\node_modules"   # junction, no admin needed
cp D:/dev2/projects/provodnik/.env.local D:/dev2/sdk-worktrees/provodnik/<task>/.env.local
```
Dispatch cursorSDK against that worktree: `... cursor-dispatch.mjs <prompt-file> --workspace D:\dev2\sdk-worktrees\provodnik\<task> --model auto` (foreground + pipefail + `source ~/.cursor-dispatch.env && export CURSOR_API_KEY`). Separate worktrees = SAFE concurrency. Cap concurrent coders at N. VERIFY each via `git -C <worktree> diff`, never the agent's summary.

**Per-task gates + review (in the worktree; may run concurrently):** `cd` into the worktree → `bun run typecheck && bun run lint && bun run test:run && bun run build` → git-diff scope-check (only named files) + trailer scan → a Sonnet reviewer (Agent, model: sonnet, subagent_type: general-purpose) → PASS/FAIL. If machine load spikes, serialize ONLY the `bun run build` step.

**Merge — STRICTLY SEQUENTIAL (where races happen):** merge only PASSED tasks, ONE AT A TIME. For each, in order:
1. `git -C <worktree> fetch origin && git -C <worktree> rebase origin/main` (picks up the previous merge; catches conflicts).
2. If the rebase changed anything, re-run gates in the worktree.
3. Push token-in-URL → PR → CI green → `gh pr merge <#> --squash --delete-branch` with your OWN clean message (no cursor/claude/co-author) → live 1280/375 check → mark `[x]`.
4. `git worktree remove D:/dev2/sdk-worktrees/provodnik/<task>` (--force if clean). GOTCHA (Windows): remove the node_modules JUNCTION first — `cmd //c rmdir "D:\dev2\sdk-worktrees\provodnik\<task>
ode_modules"` (rmdir on a junction deletes the link, not the target) — else `git worktree remove` fails with "Invalid argument".
Never run two merges in parallel; never push without a FRESH rebase (stale base makes a PR "revert" intervening merges). If a rebase conflicts (overlap slipped through), drop that task to the next wave and RE-DISPATCH it fresh against updated main — never hand-merge product code. Remove ALL wave worktrees before any repo-root gate.

## EXECUTION DETAILS (dispatch, deploy, setup, resume)
- **Parallel dispatch mechanism (critical):** run a wave's coders as PARALLEL Bash tool calls in ONE assistant message — each call independently `set -o pipefail; source ~/.cursor-dispatch.env && export CURSOR_API_KEY` so the key is live in THAT shell. Do NOT use run_in_background (detached shell drops the key); do NOT serialize. Bash tool max timeout is 600s — keep each task ≤5 files so it finishes under it; split bigger tasks. Per-worktree gates + Sonnet reviewers are also concurrent Bash/Agent calls.
- **node_modules junction + the BUILD gate (course-correct 2026-06-20):** a junctioned node_modules works for `typecheck`/`lint`/`test:run` but Turbopack `bun run build` REJECTS it ("Symlink node_modules points out of the filesystem root"). So the LOCAL worktree gate = `bun run typecheck && bun run lint && bun run test:run` only; the BUILD is verified authoritatively by the PR's CI (`.github/workflows/ci.yml` runs typecheck+build on ubuntu with real node_modules) — which is already REQUIRED green before merge. Do NOT `bun install` per worktree (slow ×48); rely on CI for build. tsc passing locally catches the vast majority of build-breakers; CI catches the rest before merge.
- **Per-task visual gate (RESOLVED 2026-06-20 -- preview bypass):** THROUGHPUT (2026-06-20): reserve the pre-merge PREVIEW browser check for SHARED/GLOBAL/high-risk tasks (nav, layout, primitives-consumers). Individual PAGE redesigns merge on Sonnet-review + CI green, then get a BATCHED prod visual check (one browser agent covering several merged pages) every ~5-6 merges, with Z01 as the authoritative final sweep — ~40 per-page browser agents would bottleneck the run. Vercel previews are SSO-protected (`ssoProtection: all_except_custom_domains`), BUT the project has an automation-bypass secret (stored at `C:/Users/x/.claude/jobs/6f1334d2/tmp/vercel-bypass.txt` -- NEVER commit or print it; not user-facing). To load a protected preview, append `?x-vercel-protection-bypass=<SECRET>&x-vercel-set-bypass-cookie=true` to the preview URL (sets a bypass cookie, then browse normally), or send header `x-vercel-protection-bypass: <SECRET>`. So DO the per-task visual check on the PR's Vercel PREVIEW @1280 AND 375 BEFORE merge -- pass the bypassed URL to the chrome-devtools agent. Z01 final sweep on live prod stays authoritative. Vercel CLI is authed on the mini (team dev-rgxges-projects) for deploy/log inspection.
- **Test accounts (all pass `Provodnik-QA-2026!`):** ADMIN = admin@provodnik.app (also admin@provodnik.test, qa+admin-20260530-063605@example.com); GUIDE = guide@provodnik.app (also qa+guide-20260530-063605@example.com); TRAVELER = traveler@provodnik.app (also qa+traveler-20260530-063605@example.com). Use these for C01 / A01-A09 / Z01. You are FULLY AUTHORIZED to create MORE users (admin/guide/traveler, verified as needed) via the mini Supabase admin API whenever a flow needs them — never block on the owner; if a creation/verification path is unclear, ask the Opus PM and proceed.
- **Resume hygiene:** the master plan + 00-INDEX.md are UNTRACKED working files in the main checkout — the `[x]` ledger is your durable progress. NEVER `git clean` / `reset --hard` / discard-checkout the MAIN checkout (it would wipe them). On resume: `git worktree prune`, remove stale `D:/dev2/sdk-worktrees/provodnik/*` dirs, delete orphan `wt-*` branches, then start the next wave from the first unchecked task.

## PROGRESS REPORTING (Telegram — keep the owner in the loop)
Send a SANITIZED Markdown update with status icons at: each WAVE START (what's being worked), each task MERGED (✅ + running X/48), any BLOCKER / DB change / PM decision (⚠️/🛠), and DONE/STOP (summary).
- **Sanitize (standing rule):** product-level only, Russian, status icons (✅ done · ⏳ in progress · 🔧 fixing · 🚀 shipped · ⚠️ blocker · 📊 progress). NO tool/model names, NO file paths, NO branch/PR/commit ids, NO internal jargon. Frame as "<page/area> — <plain outcome>".
- **Send (UTF-8 safe — Git-Bash mangles Cyrillic in inline args):** write the message to a UTF-8 file, then:
  `set -a; source D:/dev2/projects/codex-ops/.env.local; set +a; curl -s "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" --data-urlencode "chat_id=109577644" --data-urlencode "text@<file>" -d parse_mode=Markdown`
  Never hardcode or echo the token. (Bot = @ssJarvisBot, DMs owner 109577644.)

## DONE = ALL TRUE
(1) EVERY task in `docs/superpowers/plans/master-tasks/00-INDEX.md` is `[x]` (each merged to main, CI green, Sonnet PASS); (2) `bun run typecheck && bun run lint && bun run test:run && bun run build` exit 0 on main; (3) a final whole-app Sonnet browser sweep (Agent, model: sonnet, chrome-devtools) logs in as traveler + guide + admin (demo accounts, pass Provodnik-QA-2026!) and confirms EVERY redesigned page renders the canon at 1280px AND 375px with one primary CTA, real states, no emoji-glyphs / no false-empty / no <44px targets / no horizontal scroll. On DONE (or a hard 3× stop) DM the owner a sanitized product-level summary (no tool/model/file/branch names) to telegram 109577644 via the bot token in D:/dev2/projects/codex-ops/.env.local.
