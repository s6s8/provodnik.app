# Master ledger — whole-app completion (the /goal drives THIS to all-[x])
Plan + rules: docs/superpowers/plans/2026-06-20-whole-app-completion-master.md (READ it; the per-page POLISH GATE, archetype patterns, standing autonomy rules, and build-loop rules live there).
Each task: orchestrator reads this line + the master plan archetype + the REAL current page code, composes a fresh cursorSDK prompt (restyle to canon + fix the listed issues, ≤5 files, git add ONLY named files), gates, Sonnet-verifies, merges per-page to main, live-checks @1280/375, marks [x]. Order = top to bottom. cursorSDK = code; Sonnet = verify; PARALLEL fan-out via isolated worktrees in disjoint-file waves (see plan's PARALLEL FAN-OUT), merges STRICTLY sequential; branch off origin/main + rebase before each push.

## Foundations + REAL functional bugs (do first)
- [x] C01 re-audit-live      (orchestrator+Sonnet browser): log in as traveler/guide/admin (Provodnik-QA-2026!), walk every cabinet+admin page @1280/375, refresh the gap list, note any NEW broken flows beyond this ledger. ALSO audit GLOBAL CHROME (site-header/footer/guide-bottom-nav) + route-level error/404/loading surfaces, and admin login = admin@provodnik.app (pass Provodnik-QA-2026!) for A01-A09 + Z01; create MORE users (admin/guide/traveler, verified) via the mini whenever a flow needs them — fully authorized, never block, clear any blocker via the PM. Append discovered tasks here.
- [x] C02 cabinet-primitives  (cursorSDK): build/confirm shared cabinet bits used everywhere — a consistent list-row + the existing `Badge`/`EmptyState`/`PageHeader`/`LoadingSkeletons`; an `AlertDialog`-based confirm to replace native window.confirm. Build once; later tasks reuse.
- [x] C03 guide-cancel-persist (cursorSDK; maybe DB): CORRECTED (C03 research): cancel/no-show ALREADY call real server actions (declineBooking/noShowBookingAction) — they DO persist. The REAL bug is the client `nextStatusForAction` no_show gate in src/features/bookings/components/booking-detail-screen.tsx (~L895) enabling no-show in states the server rejects (only confirmed->no_show is valid per state-machine.ts). Fix the gate so no-show enables ONLY when confirmed (and cancel only from cancellable states); verify status persists in DB. ~1 file.
- [ ] C04 dead-demo-images    (cursorSDK or DB): 2 listing photos use dead Unsplash URLs (photo-1520637836862 SPB, photo-1609151354448 Сити-Чесс) — replace image_url in the seed rows with working images (DB update via mini, or fix the seed + re-point). No broken images on prod. AUDIT-B5: also the Санкт-Петербург destination card image is broken on the /trips empty state (first thing a new traveler sees) — fix it too.
- [x] C05 hydration-418       (cursorSDK): root-cause + fix the React hydration #418 (date-locale SSR/client mismatch) on request-detail (and anywhere a `toLocale*` runs in a client component) — render server-formatted date strings or a hydration-safe formatter.

- [x] C06a nav-config+header (cursorSDK, SHARED-SURFACE → SOLO wave, EARLY): implement the canonical nav IA per docs/superpowers/plans/2026-06-20-nav-ia-spec.md — CREATE src/lib/navigation.ts (SINGLE SOURCE OF TRUTH: ROUTES registry + headerPrimary/accountMenu/guideBottomNav/adminNav/footerNav + isNavActive) and rewrite site-header.tsx (+ site-header-server.tsx) + user-account-drawer.tsx to consume it: ONE label per route, role-complete header (guide gets 4 work tabs; admin gets an "Админка" bridge link), dropdown/drawer = accountMenu[role] (homes traveler Поездки/Избранное/Уведомления/Приглашения). Canon: ≥44px, Lucide only (NO glyphs), correct active states. Global imports → SOLO, merge FIRST.
- [x] C06b nav-consumers  (cursorSDK, SHARED-SURFACE → SOLO, AFTER C06a merged): rewrite guide-bottom-nav.tsx + admin-sidebar-nav.tsx + site-footer.tsx to consume src/lib/navigation.ts (guideBottomNav / adminNav / footerNav) — fix label collisions: "Заказы" (not "Подтверждённые"), "Бронирования"+shortLabel "Брони", single /trust row ("Доверие и безопасность"), "Стать гидом"→/become-a-guide, ADD /help to footer, DROP /guides from footer. Update guide-bottom-nav.test.tsx + role-layouts.test.tsx. Depends on C06a. ALSO fix `isNavActive` in src/lib/navigation.ts to RESTRICTIVE activePrefixes semantics (when activePrefixes set, skip the base startsWith — else guideInbox lights on /guide/bookings); the bottom-nav calls isNavActive directly.
- [ ] C07 route-states       (cursorSDK, may SPLIT to ≤5 files/dispatch): route-level error.tsx (auth/home/site/protected/admin) + global-error.tsx + not-found.tsx (root/site/protected) + loading.tsx (site/protected/search/messages) — canon error/404/loading: friendly Russian copy, retry/back CTA, NO raw stack/system text, canon skeletons (no false-empty), ≥44px. Split into disjoint dispatches if >5 files. AUDIT-B8: there are TWO different 404 designs (generic vs cabinet) — UNIFY them; the cabinet 404's "К входу" CTA wrongly sends authenticated users to /auth.

## Traveler cabinet
- [x] T01 /trips              : no page title; groupPrice frames a request as a group total (misleading) → PageHeader + per-person framing; consistent card style; states.
- [x] T02 /account            : traveler/guide layouts differ (containers/headings); competing jobs stacked; native multi-select → PageHeader, one canonical layout, system controls, one primary save.
- [x] T03 /notifications       (center): flat hierarchy; FALSE empty-state flash while loading; empty CardContent artifact → loading skeleton, real empty state, hierarchy, "прочитать всё" hidden at 0 unread.
- [x] T04 /messages           : no page title/toolbar; no loading/error states; jumpy hover; no next-action → PageHeader, states, calm hover, empty state with CTA.
- [ ] T05 /messages/[threadId]: input too tall (min-h-[7rem]); placeholder assumes guide; heavy glass; no empty-thread state → right-sized input, neutral placeholder, canon surface, empty state.
- [x] T06 /bookings/[id]      : multiple same-weight CTAs; mixed card styles → one primary (per booking status) + ghost others; canon cards; keep the P1 loading fix.
- [ ] T07 /bookings/[id]/dispute: generic stacked cards; error is a card not an Alert; no pending state on submit → focused form, Alert errors, pending state.
- [ ] T08 /bookings/[id]/review : five heavy star buttons; optional fields same weight as rating; no pending → clean rating control, de-emphasize optional, pending state.
- [ ] T09 /disputes/[id]      : system-centric title ("Спор #xxx"); no timeline affordance; bare empty state → human title, timeline, friendly empty.
- [ ] T10 /favorites          : listing cards NOT links; glyph icons (+ ×); empty alt text; plain-paragraph empty states → real link cards (reuse listing card), Lucide icons, real EmptyState.
- [ ] T11 /referrals          : two products merged (referral + partner API); equal-weight competing CTAs; no copy-success feedback → split/clarify one job, one primary, copy feedback.
- [x] T12 /listings/[id]/book : narrow form, no listing context/trust; Заказать/Задать вопрос equal tabs; inline price format → focused booking form WITH listing context + trust line, one primary, formatted price.

## Guide cabinet
- [x] G01 /guide + /guide/inbox: two routes render the same inbox with different titles → canonicalize (one is the canonical, the other redirects); PageHeader; calm per-card actions (one primary); kill rainbow chips → Badge; Lucide icons; ≥44px; surface load errors.
- [x] G02 /guide/profile      : too many jobs (avatar/bio/legal/licenses/verification), no single next-step → restructure as a profile CHECKLIST with a clear "next step" primary; system inputs; AlertDialog (not native confirm) for delete.
- [x] G03 /guide/listings     : primary CTA at the BOTTOM (maintenance before creation); generic text rows; glyph icons (+ ← × ✓); non-Russia placeholder ("Tbilisi"); native window.confirm → primary "Создать" at top, canon rows, Lucide, Russia placeholders, AlertDialog confirm.
- [x] G04 /guide/bookings     : no loading state; summary before the work list; missing title; muted non-prioritized cards → PageHeader, loading skeleton, work-list first, prioritized cards.
- [ ] G05 /guide/bookings/[id]: four equal operation buttons; demo-payment clutter → one primary (by state) + ghost; hide demo-payment noise. (loading fixed in P1; persistence fixed in C03.)
- [x] G06 /guide/reviews      : primary job buried; bare empty; text stars ★/☆; competing filter buttons → PageHeader, RatingDisplay-style stars, one filter pattern, friendly empty + bare-failure Alert.
- [x] G07 /guide/calendar     : two jobs combined (schedule + stats); no primary action; silent errors; cramped mobile month grid; six color dots = noise → split jobs or one primary; surface errors; mobile-usable grid; reduce dot colors to semantic.
- [x] G08 /guide/settings/contact-visibility: no primary action for a locked guide; silent error hides data failure; wrong redirect target; red X feels punitive → one clear primary path, surfaced errors, correct redirect, neutral iconography.

## Admin (one queue pattern + one row action)
- [x] A01 /admin/dashboard    : unclear primary (7 clickable cards); flat hierarchy; transform-only hover; skeleton includes sidebar → clear primary + grouped hierarchy; proper hover; content-only skeleton.
- [x] A02 /admin/moderation   : mixed jobs (listing + reply moderation); three competing row actions → split/clarify; one primary row action; (links already work via id+slug route).
- [x] A03 /admin/disputes     : implementation detail exposed ("Реальные записи"); weak text summary; long IDs as noise → hide impl detail, real summary, truncate/secondary IDs.
- [x] A04 /admin/disputes/[caseId]: three full-width competing actions; textareas use <p> not labels; no inline error; hidden admin_id visible → one primary, labelled controls, inline Alert errors, drop exposed admin_id. AUDIT-B7: the assignee renders as a raw UUID ("НАЗНАЧЕН: D0000…") — resolve it to the admin's display name.
- [x] A05 /admin/guides       : three equal actions; dense text rows; <44px buttons; ambiguous filter labels → one primary row action + menu; canon rows; ≥44px; clear filters. (badge fixed P1.)
- [x] A06 /admin/guides/[id]  : wall of same-weight cards; crowded decision buttons; no textarea label; inline style on avatar → hierarchy, one primary decision + secondary, labelled controls, no inline style.
- [x] A07 /admin/listings     : row-action competition; inline money `/100`; preview pulls out of admin context → one primary action, money formatter, slug preview links.
- [x] A08 /admin/bookings     : less finished (small H1, space-y-4); data-soup rows; raw <button>; <44px → PageHeader, structured rows, system Button, ≥44px. AUDIT-B6: rows are non-clickable plain text (no admin booking detail) + no filters/sort — make rows actionable + add filters.
- [x] A09 /admin/audit        : no filter/date/search/pagination; bare quoted notes; audit links go to /admin/listings not the specific item → add filter/search/pagination, format notes, correct deep links.

## Public marketplace + marketing + legal + auth (remaining off-canon pages — logged-out reachable)
Archetype: content/marketing page = ListHero or a focused single-column → ~85% neutral, one primary CTA, social proof where it earns trust, real Lucide icons (NO emoji glyphs), legal pages = readable long-form (max-w prose, clear headings, anchored sections), 8px rhythm, 375px-safe. Reuse `ListHero`/`PageHeader`/`Section`/`Button`/`Card`.
- [x] R01 /requests            : public marketplace LIST — unclear audience; filter uses ▾/✕ glyphs; plain-card empty state → frame the audience (guide-facing "find requests to bid on"), `ListHero` + system filter controls + Lucide chevrons, one primary CTA, real loading/empty/error states.
- [ ] S01 /become-a-guide      : wall of 6 equal cards; raw `<Link>` styled as button; no social proof → `ListHero` value framing, one primary "Стать гидом" CTA, step/benefit hierarchy, trust/social-proof block, system Button.
- [ ] S02 /how-it-works        : two equal CTAs; no visual flow → one primary CTA + secondary ghost; numbered step flow (traveler ↔ guide), canon icons, mobile-stacked.
- [ ] S03 /help               : `font-serif`; low accordion hierarchy → Onest, canon `Accordion` (shadcn) with clear hierarchy, search/contact CTA, real empty/no-results.
- [ ] S04 /trust + /for-business: double padding stacks; competing CTAs; text-heavy → tighten to one container rhythm, one primary CTA each, scannable sections, canon cards. (two pages, one task — same treatment.)
- [ ] S05 /policies/{privacy,cookies,terms}: padding stacks; walls of text; no in-page nav → one shared legal-page layout (max-w prose, sticky/section headings, last-updated line, readable line-length); apply to all three. (three pages, one task.)
- [ ] S06 /auth/forgot-password + /auth/update-password: nav-height bug; English placeholder; no back-to-login link → match the canon `/auth` single-column card, Russian copy, labelled inputs, pending+success+error states, back link. (two pages, one task.)

## Pre-existing data/query bugs (verify live first; fix only if still broken)
- [ ] B01 guide-page-query-bugs (orchestrator probe → cursorSDK/DB): the 06-17 demo-seed surfaced two — a PGRST201 ambiguous embed near `src/lib/supabase/queries.ts:817` (guide page) and a `search_guides` RPC 42804 return-type drift. Hit the live PostgREST/RPC first to confirm whether either still errors; if so, fix (disambiguate the embed with an explicit FK hint; recreate `search_guides`/rowtype in the correct order via mini) and re-verify on the live API. If both already pass, mark [x] with the verification noted — no code change.

## Internal/dev pages (decision, not redesign)
- [ ] X01 dev-pages            (orchestrator + Opus PM, maybe cursorSDK): `dev/req-cards` + `dev/guide-templates-wireframe` are internal wireframes, not user-facing. Consult the PM: delete them, or gate behind a non-prod guard so they never ship to users. Implement the PM's call; they are explicitly OUT of the canon redesign + the final sweep.

## Audit-surfaced (C01 live audit, 2026-06-20)
- [ ] B1 guide-bid-flow   (orchestrator+PM→maybe cursorSDK): audit found a guide on /guide/inbox/<id> is redirected to the PUBLIC /requests/<id> with only "Присоединиться к группе" — no guide bid form. BUT the demo guide@ is UNVERIFIED (bid buttons read "Доступно после верификации"). FIRST verify a guide (verify guide@ via the mini, or create a verified guide) and re-check whether a real bid view/action exists for VERIFIED guides. If genuinely missing → CRITICAL, PM-scope + build. If only verification-gated → mark resolved with the verified-guide evidence.
- [ ] B2 traveler-bookings-access (orchestrator+PM): no /bookings LIST route — a traveler can't see their bookings list (only /bookings/[id] from /trips). PM-decide: add a bookings list (new route or a /trips tab) vs keep inside /trips; implement the call. (Nav already drops the dead /bookings link in C06a.)
- [ ] B9 request-detail-faq-dedup (cursorSDK): the FAQ accordion is DUPLICATED on /requests/[id] (same questions render in the main content AND the footer) — remove the duplicate. (Note: by-design, NOT bugs: /favorites & /referrals 404 = intentional feature-flag-off; traveler having no mobile bottom-nav = PM decision.)

## Product-gated (consult Opus PM before building — bigger than restyle)
- [ ] P01 marketplace-gaps    (orchestrator + Opus PM): the 06-17 workflow audit flagged commodity bids, no party-size/private-group, thin off-platform-pay scaffolding. Consult the PM persona on which are worth building now; implement the ones it greenlights (each via cursorSDK + gates + Sonnet, DB via mini as needed); log deferred ones. Do NOT let this balloon DONE — PM scopes it.

## Final
- [ ] Z01 whole-app-sweep     (orchestrator+Sonnet browser): walk EVERY redesigned page @1280 AND 375 on live provodnik.app — logged-OUT public/marketing/legal/auth pages (home, listings, guides, destinations, search, /requests, become-a-guide, how-it-works, help, trust, for-business, policies/*, auth/*) AND logged-IN as traveler+guide+admin (Provodnik-QA-2026!) for every cabinet+admin page; confirm canon + one primary CTA + real states + no emoji-glyph/false-empty/<44px/h-scroll. Fix any regressions (re-dispatch), re-sweep until PASS. Then DM owner the sanitized summary.
